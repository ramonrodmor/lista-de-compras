<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rodmor.listadecompras">

    <application
        android:allowBackup="true"
        android:icon="@drawable/man_pushing_a_shopping_cart"
        android:label="@string/app_name"
        android:roundIcon="@drawable/man_pushing_a_shopping_cart"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ListaDeCompras"
            android:label="@string/title_activity_lista_de_compras"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>